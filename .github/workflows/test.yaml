name: Test

on: workflow_dispatch

jobs:
  test:

    permissions:
      id-token: write
      contents: write

    steps:
    - name: Check out repository
      uses: actions/checkout@v3
      with:
        fetch-depth: '2'

    - name: Run Tests
      id: run
      uses: /
